package com.example.michael.seg2105_project;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

public class WelcomeSceen extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_welcome_sceen);
    }
    //Nothing here yet cause i'm not to certain what to do
    //Also instead of using the recycle viewer, why not use the scroll viewer?
}
